{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCategory</th>
                <td>{{ category.idCategory }}</td>
            </tr>
            <tr>
                <th>NomCategory</th>
                <td>{{ category.nomCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">back to list</a>

    <a href="{{ path('app_category_edit', {'idCategory': category.idCategory}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
