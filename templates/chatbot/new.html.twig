{% extends 'base.html.twig' %}

{% block title %}Create New Chatbot{% endblock %}

{% block body %}
<div class="new-page">
    <h1 class="text-center">Create New Chatbot</h1>

    <div class="form-container">
        {{ include('chatbot/_form.html.twig') }}
        
        <div class="form-actions text-center">
            <a href="{{ path('app_chatbot_index') }}" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
</div>
{% endblock %}
