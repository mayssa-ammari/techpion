{% extends 'base.html.twig' %}

{% block title %}Edit Chatbot{% endblock %}

{% block body %}
<div class="edit-page">
    <h1 class="text-center">Edit Chatbot</h1>

    <div class="form-container">
        {{ include('chatbot/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="form-actions text-center">
            <a href="{{ path('app_chatbot_index') }}" class="btn btn-secondary">Back to List</a>
            {{ include('chatbot/_delete_form.html.twig', {'button_class': 'btn btn-danger'}) }}
        </div>
    </div>
</div>
{% endblock %}
